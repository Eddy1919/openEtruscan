# Files semgrep should not scan, regardless of which rule pack runs in CI.

dummy-certs/

# Tests bring in their own fixtures and assertion patterns that frequently
# trip on benign rules (psycopg2 raw-query helpers in fixture data, asserts
# on hard-coded passwords, etc.).
tests/

# CI/CD config files — already audited by GitHub Actions / CodeQL.
.github/

# The legacy sync `Corpus` class in core/corpus.py builds parameterised
# Postgres queries with f-strings whose string component is a static
# constant (`_INSCRIPTION_COLS`, the radius/MVT templates). The SQLAlchemy
# raw-query rule cannot tell that the f-string substitution comes from a
# constant rather than user input, so it flags every usage. We've inspected
# the call sites; user-supplied values are always passed as %s parameters.
# Suppressing project-wide for this file.
src/openetruscan/core/corpus.py
